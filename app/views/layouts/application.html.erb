<!DOCTYPE html>
<html>
  <head>
    <title>LibrarySystem</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= vite_client_tag %>
    <%= vite_javascript_tag "application.jsx" %>
    <!--
      If using a TypeScript entrypoint file:
        vite_typescript_tag 'application'

      If using a .jsx or .tsx entrypoint, add the extension:
        vite_javascript_tag 'application.jsx'

      Visit the guide for more information: https://vite-ruby.netlify.app/guide/rails
    -->

  </head>

  <body>
    <!-- Flash Messages -->
    <% if notice %>
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4">
        <%= notice %>
      </div>
    <% end %>
    
    <% if alert %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
        <%= alert %>
      </div>
    <% end %>

    <!-- Navigation -->
    <nav class="bg-blue-600 text-white p-4 mb-6">
      <div class="container mx-auto flex justify-between items-center">
        <h1 class="text-xl font-bold">Library Management System</h1>
        
        <div class="flex space-x-4">
          <% if user_signed_in? %>
            <span>Welcome, <%= current_user.full_name %> (<%= current_user.role.titleize %>)</span>
            <%= link_to "Dashboard", dashboard_path, class: "hover:underline" %>
            <%= link_to "Books", books_path, class: "hover:underline" %>
            <%= link_to "My Loans", loans_path, class: "hover:underline" %>
            <% if current_user.librarian? %>
              <%= link_to "Add Book", new_book_path, class: "hover:underline" %>
            <% end %>
            <%= link_to "Edit Profile", edit_user_registration_path, class: "hover:underline" %>
            <%= form_with url: destroy_user_session_path, method: :delete, local: true, class: "inline" do |form| %>
              <%= form.submit "Logout", class: "hover:underline bg-transparent border-none cursor-pointer text-white" %>
            <% end %>
          <% else %>
            <%= link_to "Login", new_user_session_path, class: "hover:underline" %>
            <%= link_to "Sign Up", new_user_registration_path, class: "hover:underline" %>
          <% end %>
        </div>
      </div>
    </nav>

    <main class="container mx-auto px-4">
      <%= yield %>
    </main>
    
    <div id="root"></div>
  </body>
</html>
